# 隐私与数据说明

当前 macOS QQ 版本读取可见文字及有名称的表情节点并进行 Jev 结构化分析，不生成、填入或发送回复。

## 留在本机的数据

- QQ 辅助功能树只在内存中读取，不保存聊天快照或数据库副本。
- 最近已见消息与判断结果仅在进程内缓存：每会话最多 32 条、最多 24 个会话、最多 128 个判断；退出即消失。
- 正常诊断日志不主动记录聊天标题、昵称、消息正文或模型输入；分享日志前仍应检查其中的错误信息。
- 窗口位置、消息位置和内容指纹仅用于当前进程内的去重与界面定位。
- 当前 HUD 只读取 Decision Infra 地址和精确路由作为判断配置；Provider Key 应只保存在 infra 项目中。
- 默认用户配置路径沿用旧应用名 `~/.config/jev-jarvis/env`，设置界面保存时将文件权限设为 `600`。

## Decision Infra 数据流向

当前可视区里每条对方消息分别与最多 8 条、2000 个 Unicode 字符的最近已见上下文发送给本机 Decision Infra；自己的消息可进入上下文，但不作为分析目标。若 QQ 的辅助功能树明确标出引用容器，被引用文字会作为单独的背景字段随该条消息发送；否则可能仍混在正文里：

```text
QQ 辅助功能树 → 本机 127.0.0.1:8080/v1/systemone → infra 选定的 Provider
```

默认精确路由是 `jev-latest`。QQ 应用不会读取 `TYPESAFE_API_KEY`；该 Key 只由 Decision Infra 进程读取。应用不会在路由失败时静默改用其他模型。

发送目的是取得意图、行为、情绪、当前需求、隐含请求/催促/边界信号、风险评分、现在回复是否合适的概率，以及多个行动策略的适配分。程序不会把消息发送给回复生成模型。后续是否出网以及数据保存规则取决于 Decision Infra 实际选择的 Provider；使用托管 Jev 时应按其隐私政策评估。

## 不会执行的操作

- 不截图或 OCR QQ 窗口。
- 不读取、解密或复制 QQ 数据库。
- 不注入、hook 或修改 QQ 进程。
- 不自动滚动聊天历史。
- 不调用回复生成模型。
- 不复制、填入或发送消息。

请只在本人设备和本人账号上使用，不要用它读取他人的聊天内容。
