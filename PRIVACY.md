# 隐私与数据说明

macOS 与 Android 版本都读取 QQ 当前聊天的可见文字消息并进行 Jev 结构化分析；可识别的图片、贴图和文件仅显示固定类型占位，不显示媒体内容或文件名，也不发送给模型。不生成、填入或发送回复。

文件占位依赖 QQ 辅助功能树提供明确的文件类型标记。如果某个 QQ 版本只暴露文件名为普通文字、没有类型标记，应用无法可靠区分它与真正的文字消息；遇到这种情况需要针对该版本适配。

## 留在本机的数据

- QQ 辅助功能树只在内存中读取，不保存聊天快照或数据库副本。
- 最近已见消息与判断结果仅在进程内缓存：每会话最多 32 条、最多 24 个会话、最多 128 个判断；退出即消失。
- 正常诊断日志不主动记录聊天标题、昵称、消息正文或模型输入；分享日志前仍应检查其中的错误信息。
- 窗口位置、消息位置和内容指纹仅用于当前进程内的去重与界面定位。
- macOS HUD 只读取 Decision Infra 地址和精确路由作为判断配置；Provider Key 应只保存在 infra 项目中。
- 默认用户配置路径沿用旧应用名 `~/.config/jev-jarvis/env`，设置界面保存时将文件权限设为 `600`。

Android 端也只在内存中读取 QQ 无障碍树，沿用每会话最多 32 条、最多 24 个会话、最多 128 个判断的进程内缓存。设置保存在应用私有存储中；直连 Jev 的 API Key 由 Android Keystore 加密，备份已关闭。设置页不回显已保存的 Key。更换 URL、模型、模式或 Key 后，旧判断缓存会清空。

Android 的兼容性诊断需手动点“准备一次 QQ 节点采集”，只采集随后打开的一个 QQ 聊天页。应用私有存储中的诊断报告记录节点 ID、控件类型、位置、层级及文字/描述是否存在，附带设备型号、Android API 和 QQ 版本；**不保存节点的文字或描述内容**，也不保存 API Key。再次准备采集会清除旧报告。点“导出”后，报告由用户选择保存位置，是否分享由用户决定。

## macOS Decision Infra 数据流向

当前可视区里每条对方文字消息分别与最多 8 条、2000 个 Unicode 字符的最近已见文字上下文发送给本机 Decision Infra；媒体占位不会进入上下文。自己的文字消息可进入上下文，但不作为分析目标。若 QQ 的辅助功能树明确标出引用容器，被引用文字会作为单独的背景字段随该条消息发送；否则可能仍混在正文里：

```text
QQ 辅助功能树 → 本机 127.0.0.1:8080/v1/systemone → infra 选定的 Provider
```

默认精确路由是 `jev-latest`。QQ 应用不会读取 `TYPESAFE_API_KEY`；该 Key 只由 Decision Infra 进程读取。应用不会在路由失败时静默改用其他模型。

发送目的是取得意图、行为、情绪、当前需求、隐含请求/催促/边界信号、回复风险、此刻是否值得回复的判断值，以及多个行动策略的适配分。程序不会把消息发送给回复生成模型。后续是否出网以及数据保存规则取决于 Decision Infra 实际选择的 Provider；使用托管 Jev 时应按其隐私政策评估。

## Android 数据流向

Android 服务仅在 QQ 位于前台时读取当前聊天的可见节点。对方文字与最多 8 条、2000 字符的已见文字上下文逐条发送；自己的文字只可进入上下文，可识别媒体不进入请求，方向不明和标题不明时不发起判断。明确标出的引用文字作为单独的 `quote` 背景字段。离开 QQ 时隐藏面板，未完成的旧结果不会显示。

- **Jev API 模式：**消息发送到用户在设置页填写的 HTTPS `/v1/systemone` URL，使用该 URL 对应的 Bearer API Key。默认 URL 为 `https://api.typesafe.ai/v1/systemone`，模型路由为 `jev-latest`。外部 HTTP 地址会被拒绝，HTTP 重定向不会被跟随。应用不会读取或发送 QQ 账号密码。
- **Decision Infra 模式：**消息发送到用户填写的网关 URL；默认 `http://127.0.0.1:8080/v1/systemone` 指手机本机。通过 USB 的 `adb reverse tcp:8080 tcp:8080` 可接入 Mac 上的网关。Android 应用不向网关发送已保存的 Jev API Key；Provider Key 由网关管理。

“测试连接”会发送应用内固定的一条合成问候到当前选择的判断服务；它不读取 QQ。无障碍权限需要由用户在 Android 系统设置中开启。

## 不会执行的操作

- 不截图或 OCR QQ 窗口。
- 不读取、解密或复制 QQ 数据库。
- 不注入、hook 或修改 QQ 进程。
- 不自动滚动聊天历史。
- 不调用回复生成模型。
- 不复制、填入或发送消息。

请只在本人设备和本人账号上使用，不要用它读取他人的聊天内容。
